# AGENTS.md

## Cursor Cloud specific instructions

This is currently an empty repository with only a `README.md` placeholder. There are no application services, dependencies, build systems, or test frameworks configured.

- **No package manager lockfile** exists — no `package-lock.json`, `yarn.lock`, `pnpm-lock.yaml`, `requirements.txt`, `go.mod`, etc.
- **No build/run scripts** — no `Makefile`, `Dockerfile`, `docker-compose.yml`, or shell scripts.
- **No linting or testing** frameworks are configured.

When application code is added, update this file with relevant startup, build, test, and lint instructions.
